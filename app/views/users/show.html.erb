<div class="container">
  <div>
    <h2 class="header">Welcome <%= "#{@user.first_name} #{@user.last_name}" %></h2>
  </div>

  <% if @user.reciever %>
    <div class="card">
      <div class="card-body">
        <p>
          You will be giving to <%= "#{@user.reciever.first_name} #{@user.reciever.last_name}" %>
        </p>
      </div>
    </div>
  <% end %>
</div>
